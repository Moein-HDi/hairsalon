@model hairsalon.Models.CheckPhoneVM
@{
    ViewBag.Title = "ورود به حساب";
    Layout = "~/Views/Shared/_Layout.cshtml";

}


@Html.Partial("_PartialHeader")


<!-- ======= profile Section ======= -->
<section id="profile" class="profile">
    <div class="container" data-aos="fade-up">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card" style="padding: 20px;">

                    <div>
                        @using (Html.BeginForm("Index", "Account", FormMethod.Post))
                        {


                            <div class="mb-3">

                                <label for="mobileInput" class="mb-1">شماره موبایل خود را وارد کنید:</label>
                                @Html.TextBoxFor(m => m.PhoneNumber, new { @class = "form-control rounded-3", @maxlength = "11", @id = "mobileInput", @placeholder = "مثال: 09123456789" })
                                @Html.ValidationMessageFor(m => m.PhoneNumber, "", new { @class = "text-danger" })
                            </div>

                            <input value="ورود به حساب کاربری" class="w-100 btn btn-lg rounded-3 btn-primary" type="submit" />

                        }
                    </div>

                </div>
            </div>
        </div>
    </div>
</section><!-- End profile Section -->
